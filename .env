# Vacío a propósito: usamos el proxy de Vite (ver vite.config.js) para
# que /auth, /products, etc. sean same-origin y no haya CORS.
VITE_API_BASE_URL=

# Mocks para todo el resto de la app...
VITE_USE_MOCK=true

# ...excepto login, que va contra el backend real (wh-backend en :8080).
VITE_USE_MOCK_AUTH=false
